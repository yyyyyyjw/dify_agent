## 一、启动系统
### 1.1 启动服务
代码根目录（/dify_agent）下运行：
```bash
docker-compose up -d --build
```
待服务启动成功后，在浏览器访问：
```
http://192.168.101.196:4000
```
### 1.2 停止服务

```bash
docker-compose down
```

---

## 二、用户使用说明
### 1. 账号管理
#### 注册账号
1. 若初次使用该系统，请点击登录页面下方的 **"立即注册"** 。
2. 填写用户名、邮箱和密码后提交。
3. 注册成功后自动跳转至登录页。
> 用户名和邮箱均唯一，不可重复注册。

#### 登录
1. 在登录页输入**用户名或邮箱**以及密码。
2. 点击 **"立即登录"**。
3. 登录成功后进入主对话界面。
> 忘记密码时，请联系管理员进行重置。

#### 修改密码
登录后，点击侧边栏下方的**修改密码**，输入原密码和新密码后确认。

---

### 2. 对话功能
#### 发起新对话
1. 点击侧边栏顶部的 **"新对话"** 按钮。
2. 在底部输入框输入问题。
3. 点击发送按钮（或按 `Enter` 键）提交。
4. AI 回复将以**流式打字效果**实时显示。

#### 切换历史对话
在左侧侧边栏点击任意历史对话，即可加载该对话的完整消息记录。

#### 等待响应期间
消息发送后，发送按钮会变为加载状态，期间不可重复提交同一对话的请求，避免并发混乱。

---

### 3. 评分与反馈
#### 评价反馈
每条 AI 回复下方提供反馈入口：
1. 点击回复消息下方的**评分按钮**（初始时默认展开）。
2. 选择 1～10 分的评分（1 分最低，10 分最高）。
3. 可选填写文字评论。
4. 点击提交评价按钮。

#### 修改评价
提交后可以点击评价右上角的铅笔图标对之前的评价进行修改与二次提交。

---


## 三、管理员使用说明
### 1. 获取管理员权限
新注册账号默认为普通用户，目前暂时还需通过数据库命令手动授权。
启动服务后，在终端输入：
```bash
docker exec -it dify_agent_db bash
psql -U postgres -d dify_agent
UPDATE users SET is_admin = true WHERE username = '目标用户名';
```
### 2. 管理员后台
登录管理员账号后，可在前端管理页面使用以下功能：

#### 查看所有对话

- 浏览所有用户的全部对话及消息内容。
- 支持按**评分区间**（1～10 分）过滤，快速定位高分/低分对话。

#### 用户活跃度统计

- **每日提问统计**：查看指定日期内所有用户的提问数量及对应会话列表。
- **月度热力图**：以GitHub贡献图风格展示每位用户某月每天的提问频率。

#### 用户排行榜

查看所有用户按总提问次数排列的排行榜。

#### 用户管理
- **手动设置会员等级**：可将任意用户的等级设置为任意等级。
- **重置用户密码**：直接为指定用户设置新密码（无需知晓原密码）。

---

## 四、测试账号
### 用户账号
- username：linux
- email：linux@linux.com
- password：123456
### 管理员账户
- username：admin
- email：admin@admin.com
- password：123456