# 用户使用手册

> 本系统是一个集成 Dify 工作流 AI 的全栈对话平台，支持多轮流式对话、历史记录持久化、消息评分反馈及会员等级体系。

---

## 目录

1. [系统架构概览](#1-系统架构概览)
2. [部署与启动](#2-部署与启动)
3. [账号管理](#3-账号管理)
4. [使用对话功能](#4-使用对话功能)
5. [评分与反馈](#5-评分与反馈)
6. [会员等级体系](#6-会员等级体系)
7. [管理员功能](#7-管理员功能)
8. [常用维护命令](#8-常用维护命令)
9. [常见问题](#9-常见问题)

---

## 1. 系统架构概览

| 组件 | 技术栈 | 说明 |
|------|--------|------|
| 前端 | Next.js (React) | 唯一对外暴露的入口，监听 `3000` 端口 |
| 后端 | FastAPI (Python) | 仅供前端容器内部访问，监听 `8000` 端口 |
| 数据库 | PostgreSQL 15 | 存储用户、对话、消息、反馈数据 |
| AI 引擎 | Dify Chatflow / Workflow | 通过 API Key 调用，支持 SSE 流式返回 |

**网络访问路径：**

```
用户浏览器 → 前端 (3000) → 后端 (8000) → Dify API
```

后端不对外暴露端口，所有请求由 Next.js 服务端代理转发，保证安全性。

---

## 2. 部署与启动

### 2.1 环境要求

- Docker & Docker Compose
- （可选）Python 3.10+ / Node.js 18+（仅本地开发时需要）

### 2.2 配置环境变量

**第一步：** 在项目根目录创建 `.env` 文件：

```env
# 数据库配置
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=dify_agent
```

**第二步：** 在 `backend/` 目录下创建 `.env` 文件：

```env
DATABASE_URL=postgresql://postgres:your_password@db:5432/dify_agent
SECRET_KEY=your_secret_key          # 用于 JWT 签名，请设置为随机字符串
DIFY_API_KEY=your_dify_api_key      # 从 Dify 平台获取
DIFY_API_URL=http://host.docker.internal:端口/v1  # Dify 服务地址
```

**第三步：** 修改 `docker-compose.yml` 中的前端端口绑定，将 `192.168.101.133` 替换为本机实际内网 IP（Windows 下运行 `ipconfig` 查看）：

```yaml
ports:
  - "你的内网IP:3000:3000"
```

### 2.3 一键启动

```bash
docker-compose up -d --build
```

启动成功后，在浏览器访问：

```
http://你的内网IP:3000
```

### 2.4 停止服务

```bash
docker-compose down
```

---

## 3. 账号管理

### 3.1 注册账号

1. 打开系统首页，点击登录页面下方的 **"立即注册"** 链接。
2. 填写用户名、邮箱和密码后提交。
3. 注册成功后自动跳转至登录页。

> 用户名和邮箱均唯一，不可重复注册。

### 3.2 登录

1. 在登录页输入**用户名或邮箱**以及密码。
2. 点击 **"立即登录"**。
3. 登录成功后进入主对话界面。

> 忘记密码时，请联系管理员进行重置。

### 3.3 修改密码

登录后，在对话界面点击右上角的用户头像或设置按钮，选择**修改密码**，输入原密码和新密码后确认。

---

## 4. 使用对话功能

### 4.1 界面布局

```
┌─────────────────────────────────────────┐
│  侧边栏（历史对话列表）  │  主对话区域   │
│                          │               │
│  [+ 新建对话]            │  消息气泡     │
│  ─────────────────       │  ...          │
│  对话 1                  │               │
│  对话 2                  │               │
│  ...                     │  [输入框] [发送] │
└─────────────────────────────────────────┘
```

### 4.2 发起新对话

1. 点击侧边栏顶部的 **"+ 新建对话"** 按钮。
2. 在底部输入框输入问题。
3. 点击发送按钮（或按 `Enter` 键）提交。
4. AI 回复将以**流式打字效果**实时显示。

> 新对话的标题会自动取问题的前 20 个字符生成。

### 4.3 切换历史对话

在左侧侧边栏点击任意历史对话，即可加载该对话的完整消息记录，并在此基础上继续提问（支持多轮上下文）。

### 4.4 消息格式

AI 回复支持 **Markdown 渲染**，包括：

- 标题、加粗、斜体等文本格式
- 有序/无序列表
- 代码块（语法高亮）
- 表格

### 4.5 等待响应期间

消息发送后，发送按钮会变为加载状态，期间不可重复提交同一对话的请求，避免并发混乱。

---

## 5. 评分与反馈

每条 AI 回复下方提供反馈入口：

1. 点击回复消息旁的**评分按钮**。
2. 选择 1～10 分的评分（1 分最低，10 分最高）。
3. 可选填写文字评论。
4. 点击提交。

反馈数据将被持久化保存，管理员可在后台按评分区间筛选查看。

---

## 6. 会员等级体系

系统根据用户的**累计 AI 有效提问次数**自动升级会员等级（只升不降）：

| 等级 | 标识 | 所需提问次数 |
|------|------|-------------|
| 免费用户 | free | 0 次 |
| 青铜 | bronze | ≥ 1 次 |
| 白银 | silver | ≥ 10 次 |
| 黄金 | gold | ≥ 30 次 |
| 钻石 | diamond | ≥ 50 次 |
| 王者 | king | ≥ 100 次 |

> 每次 AI 成功响应后自动触发等级检查与更新。管理员也可手动调整用户等级。

---

## 7. 管理员功能

### 7.1 获取管理员权限

新注册账号默认为普通用户。需通过数据库命令手动授权：

```bash
docker exec -it dify_agent_db bash
psql -U postgres -d dify_agent
UPDATE users SET is_admin = true WHERE username = '目标用户名';
```

### 7.2 管理员后台功能

登录管理员账号后，可在前端管理页面（通常为 `/admin` 路由）使用以下功能：

#### 查看所有对话

- 浏览所有用户的全部对话及消息内容。
- 支持按**评分区间**（1～10 分）过滤，快速定位高分/低分对话。

#### 用户活跃度统计

- **每日提问统计**：查看指定日期内所有用户的提问数量及对应会话列表。
- **月度热力图**：以 GitHub 贡献图风格展示每位用户某月每天的提问频率。

#### 用户排行榜

查看所有用户按总提问次数排列的排行榜。

#### 用户管理

- **手动设置会员等级**：可将任意用户的等级设置为 `free / bronze / silver / gold / diamond / king`。
- **重置用户密码**：直接为指定用户设置新密码（无需知晓原密码）。

---

## 8. 常用维护命令

### 重新部署（清空数据）

```bash
# Windows (PowerShell)
Remove-Item -Recurse -Force ./data
docker-compose up -d --build

# Linux / macOS
rm -rf ./data/db
docker-compose up -d --build
```

> **警告：** 此操作将清空所有数据库数据，请谨慎执行。

### 进入数据库手动操作

```bash
docker exec -it dify_agent_db bash
psql -U postgres -d dify_agent
```

常用 SQL 示例：

```sql
-- 查看所有用户
SELECT id, username, email, is_admin, membership_level FROM users;

-- 授予管理员权限
UPDATE users SET is_admin = true WHERE username = 'admin';

-- 查看对话数量
SELECT COUNT(*) FROM conversations;
```

### 查看服务日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 只看后端日志
docker-compose logs -f backend

# 只看前端日志
docker-compose logs -f frontend
```

### 仅重启某个服务

```bash
docker-compose restart backend
docker-compose restart frontend
```

---

## 9. 常见问题

**Q：页面打开空白或报错 "无法连接服务器"？**

A：检查 `docker-compose.yml` 中的 IP 地址是否为本机当前内网 IP（`ipconfig` 查看），IP 变化后需修改并重新 `up --build`。

---

**Q：AI 一直转圈不响应？**

A：检查 `backend/.env` 中 `DIFY_API_KEY` 和 `DIFY_API_URL` 是否配置正确，以及 Dify 服务是否正常运行。可通过 `docker-compose logs -f backend` 查看错误信息。

---

**Q：登录提示"用户名或密码错误"？**

A：支持使用**用户名**或**注册邮箱**登录，请确认输入正确。忘记密码请联系管理员通过后台重置。

---

**Q：如何让新用户无法自行注册（仅管理员创建账号）？**

A：目前注册接口为开放状态。如需关闭，可在后端 `backend/app/api/auth.py` 中注释掉 `/register` 路由，然后重新部署后端。

---

**Q：Dify 与本系统部署在同一台机器上，如何配置 URL？**

A：在 `backend/.env` 中将 `DIFY_API_URL` 设为：

```env
DIFY_API_URL=http://host.docker.internal:你的Dify端口/v1
```

`host.docker.internal` 是 Docker 容器访问宿主机的特殊域名，Windows/macOS 下默认支持，Linux 需在 `docker-compose.yml` 的 `backend` 服务下添加 `extra_hosts: ["host.docker.internal:host-gateway"]`。
